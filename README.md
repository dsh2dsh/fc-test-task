# Usage:
```
 -i string
        name of input .csv file
  -input string
        name of input .csv file
  -lowmem
        slower, but use less RAM
  -o string
        dir for output .csv files (default ".")
  -output string
        dir for output .csv files (default ".")
```
